import React from "react";

const SettingsNav = ({}) => {
    return null;

    return (
        <div className="settings navigation">
            <section>
                <nav>
                    <li>
                        <div
                            className={"item" + __class(false, "active")}
                            onClick={() => console.log(value)}
                        >
                        </div>
                    </li>
                </nav>
            </section>
        </div>
    );
};

export default SettingsNav;
