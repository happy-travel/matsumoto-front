import { session, localStorage } from "./storage";
import { getParams } from "./misc/get-params";
import API from "./api";
import { setLocale, getLocale } from "./misc/locale";
import { redirect } from "./misc/history";

export {
    session,
    localStorage,
    getParams,
    API,
    setLocale,
    getLocale,
    redirect
};
